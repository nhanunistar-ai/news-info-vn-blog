---
import Slide1 from '../assets/the-grey.png';
import Slide2 from '../assets/chapter-1.png';
import Slide3 from '../assets/chapter-2.png';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="vi">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero-slider" aria-label="Trang chu">
				<div class="slides">
					<article class="slide" style={`background-image: url(${Slide1.src});`}>
						<div class="overlay"></div>
						<div class="content">
							<p class="subtitle">Bí ẩn về UFO và Người ngoài hành tinh</p>
							<h1>ALIEN INTERVIEW</h1>
							<p class="desc">Điều gì đúng với bạn, thì nó đúng với bạn</p>
							<div class="actions">
								<a href="/blog/alien-interview" class="btn btn-line">Đọc ngay</a>
							</div>
						</div>
					</article>
					<article class="slide" style={`background-image: url(${Slide2.src});`}>
						<div class="overlay"></div>
						<div class="content">
							<p class="subtitle">ALIEN INTERVIEW</p>
							<h1>CHAPTER 1</h1>
							<p class="desc">Cuộc phỏng vấn đầu tiên</p>
							<div class="actions">
								<a href="/blog/doi-thoai-than-giao-cach-cam-voi-thuc-the-roswell/" class="btn btn-line">Đọc ngay</a>
							</div>
						</div>
					</article>
					<article class="slide" style={`background-image: url(${Slide3.src});`}>
						<div class="overlay"></div>
						<div class="content">
							<p class="subtitle">ALIEN INTERVIEW</p>
							<h1>CHAPTER 2</h1>
							<p class="desc">Đối Thoại Thần Giao Với Thực Thể Roswell</p>
							<div class="actions">
								<a href="/blog/doi-thoai-than-giao-voi-thuc-the-roswell/" class="btn btn-line">Đọc ngay</a>
							</div>
						</div>
					</article>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	main {
		width: 100%;
		max-width: 100%;
		padding: 0;
		margin: 0;
	}
	.hero-slider {
		position: relative;
		width: 100%;
		height: clamp(420px, 78vh, 760px);
		overflow: hidden;
	}
	.slides {
		display: flex;
		width: 300%;
		height: 100%;
		animation: slideX 18s infinite;
	}
	.slide {
		position: relative;
		width: 33.3333%;
		height: 100%;
		background-size: cover;
		background-position: center;
	}
	.overlay {
		position: absolute;
		inset: 0;
		background: rgba(0, 0, 0, 0.45);
	}
	.content {
		position: relative;
		z-index: 1;
		height: 100%;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		text-align: center;
		color: #fff;
		padding: 1.5rem;
	}
	.subtitle {
		font-size: 1.1rem;
		margin-bottom: 0.75rem;
	}
	h1 {
		color: #fff;
		font-size: clamp(2rem, 5vw, 4rem);
		line-height: 1.08;
		margin-bottom: 0.8rem;
	}
	.desc {
		font-size: 1.2rem;
		max-width: 680px;
		margin-bottom: 1.3rem;
	}
	.actions {
		display: flex;
		gap: 0.8rem;
		flex-wrap: wrap;
		justify-content: center;
	}
	.btn {
		padding: 0.75rem 1.35rem;
		border-radius: 8px;
		text-decoration: none;
		font-weight: 700;
		border: 1px solid transparent;
	}
	.btn-solid {
		background: #0bb08b;
		color: #fff;
	}
	.btn-line {
		border-color: rgba(255, 255, 255, 0.8);
		color: #fff;
	}
	@keyframes slideX {
		0%, 30% { transform: translateX(0); }
		33%, 63% { transform: translateX(-33.3333%); }
		66%, 96% { transform: translateX(-66.6666%); }
		100% { transform: translateX(0); }
	}
	@media (max-width: 720px) {
		.hero-slider {
			height: 72vh;
		}
		.desc {
			font-size: 1rem;
		}
	}
</style>
